# 开发环境配置
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.school.grade: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
